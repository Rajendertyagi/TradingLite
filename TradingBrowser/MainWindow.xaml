<Window x:Class="TradingBrowser.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:TradingBrowser.ViewModels"
        Title="TradingBrowser" Height="800" Width="1200"
        Background="{DynamicResource SystemControlBackgroundAltHighBrush}"
        WindowStyle="None" AllowsTransparency="False" ResizeMode="CanResize"
        Loaded="Window_Loaded">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 1. Title Bar -->
        <Grid Grid.Row="0" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="TradingBrowser" FontSize="12" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="─" Width="46" Height="32" Style="{StaticResource CaptionButtonStyle}" Click="Minimize_Click"/>
                <Button Content="☐" Width="46" Height="32" Style="{StaticResource CaptionButtonStyle}" Click="Maximize_Click"/>
                <Button Content="✕" Width="46" Height="32" Style="{StaticResource CloseButtonStyle}" Click="Close_Click"/>
            </StackPanel>
        </Grid>

        <!-- 2. Tab Strip (Now Data Bound to MVVM) -->
        <Border Grid.Row="1" Height="36" Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}" BorderThickness="0,0,0,1" BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}">
            <StackPanel Orientation="Horizontal">
                <ItemsControl ItemsSource="{Binding Tabs}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{DynamicResource SystemControlBackgroundChromeHighBrush}" 
                                    Margin="2,4,0,4" CornerRadius="6,6,0,0" 
                                    Width="200" Height="28" 
                                    VerticalAlignment="Bottom"
                                    MouseLeftButtonDown="Tab_Click">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Title}" Margin="10,0,0,0" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                    <Button Grid.Column="1" Content="✕" FontSize="10" Width="20" Height="20" 
                                            Style="{StaticResource CaptionButtonStyle}" 
                                            Command="{Binding DataContext.CloseTabCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                                            CommandParameter="{Binding}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
                <Button Content="+" Width="36" Height="36" FontWeight="Bold" Style="{StaticResource CaptionButtonStyle}" 
                        Command="{Binding AddTabCommand}"/>
            </StackPanel>
        </Border>

        <!-- 3. Address Bar -->
        <Border Grid.Row="2" Height="40" Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}" Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="1" Text="{Binding SelectedTab.Url}" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="{DynamicResource SystemControlForegroundBaseMediumLowBrush}"/>
            </Grid>
        </Border>

        <!-- 4. Active WebView Host -->
        <ContentControl Grid.Row="3" x:Name="ActiveWebViewHost"/>
        
        <!-- Hidden Pool: Keeps background tabs alive so they don't discard instantly -->
        <Grid Grid.Row="3" x:Name="HiddenWebViewPool" Visibility="Collapsed"/>
    </Grid>
</Window>
